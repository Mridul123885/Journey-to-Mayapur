node_modules/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/captures/
android/.idea/
*.iml
.DS_Store

# NOTE: android/app/release.keystore is intentionally NOT ignored.
# It is committed on purpose so every GitHub Actions build produces a
# consistently-signed, installable APK/AAB with zero extra setup.
# See README.md "About the signing key" before publishing to Google Play.
